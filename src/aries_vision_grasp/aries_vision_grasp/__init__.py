"""Shared library for the aries_vision_grasp nodes.

Modules:
  geometry  — quaternion/rotation/duration helpers (pure functions)
  fourbar   — calibrated four-bar gripper gap and contact-offset models
  stages    — grasp state-machine stage names and shared stage sets
  inference — YOLO model loading and background-thread inference
"""
