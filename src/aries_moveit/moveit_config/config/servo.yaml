###############################################
# Modify all parameters related to servoing here
###############################################

# Optionally override Servo's internal velocity scaling when near singularity or collision (0.0 = use internal velocity scaling)
# override_velocity_scaling_factor = 0.0 # valid range [0.0:1.0]

## Properties of outgoing commands
publish_period: 0.0125
# max_expected_latency: 0.5  # delay between sending a command and the robot executing it [seconds]
incoming_command_timeout: 0.04

command_in_type: "speed_units"  # "unitless"> in the range [-1:1], as if from joystick. "speed_units"> cmds are in m/s and rad/s
scale:
  # Scale parameters are only used if command_in_type=="unitless"
  linear:  1.0  # Max linear velocity. Unit is [m/s]. Only used for Cartesian commands.
  rotational:  1.0  # Max angular velocity. Unit is [rad/s]. Only used for Cartesian commands.
  # Max joint angular/linear velocity. Only used for joint commands on joint_command_in_topic.
  joint: 1.0

# What type of topic does your robot driver expect?
# Currently supported are std_msgs/Float64MultiArray or trajectory_msgs/JointTrajectory
command_out_type: trajectory_msgs/JointTrajectory

# What to publish? Can save some bandwidth as most robots only require positions or velocities
publish_joint_positions: true
publish_joint_velocities: true
publish_joint_accelerations: false

## Plugins for smoothing outgoing commands
# Smoothing is OFF: adding a filter here creates onset lag (arm starts moving
# after stick push, not during).  Stop-smoothing is handled in the joystick
# node itself with a velocity ramp-down so instant start is preserved.
use_smoothing: false
smoothing_filter_plugin_name: "online_signal_smoothing::ButterworthFilterPlugin"

# If is_primary_planning_scene_monitor is set to true, the Servo server's PlanningScene advertises the /get_planning_scene service,
# which other nodes can use as a source for information about the planning environment.
# NOTE: If a different node in your system is responsible for the "primary" planning scene instance (e.g. the MoveGroup node),
# then is_primary_planning_scene_monitor needs to be set to false.
is_primary_planning_scene_monitor: false

## MoveIt properties
move_group_name: igus_rebel_arm  # Often 'manipulator' or 'arm'

## Configure handling of singularities and joint limits
lower_singularity_threshold: 600.0  # Start decelerating when the condition number hits this (close to singularity)
hard_stop_singularity_threshold: 900.0  # Stop when the condition number hits this
leaving_singularity_threshold_multiplier: 10.0  # Multiply the hard stop limit by this when leaving singularity
# Added as a buffer to joint variable position bounds [in that joint variable's respective units].
# Can be of size 1, which applies the margin to all joints, or the same size as the number of degrees of freedom of the active joint group.
# If moving quickly, make these values larger.
joint_limit_margins: [0.03]

## Topic names
cartesian_command_in_topic: delta_twist_cmds  # Topic for incoming Cartesian twist commands
joint_command_in_topic: delta_joint_cmds  # Topic for incoming joint angle commands
joint_topic: joint_states  # Get joint states from this topic
status_topic: ~/status  # Publish status to this topic
command_out_topic: servo_guard/input_joint_trajectory  # Guard checks this before forwarding to the arm controller

## Collision checking for the entire robot body
check_collisions: true
collision_check_rate: 40.0
self_collision_proximity_threshold: 0.015
scene_collision_proximity_threshold: 0.020
