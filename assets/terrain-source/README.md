# Terrain Source Assets

Large source imagery and intermediate terrain files used to prepare the Aries
Gazebo terrain. These files are kept outside the `aries` ROS package because
the simulator does not load them at runtime.

Runtime terrain files remain in `src/aries/models/dem/`.
