# Legacy Configuration

Configuration retained for reference but not installed with an active ROS
package. `joint_names_ROS.yaml` contains an early exported link-name list; it
is not a valid controller joint configuration for the current robot.
