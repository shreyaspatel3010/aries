# Mesh Alternatives

Geometry variants retained for future optimization but not referenced by the
current Aries URDF/Xacro files. Keeping them outside `src/aries/meshes/` avoids
installing unused geometry with the runtime ROS package.
